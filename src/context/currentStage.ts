// import React, { createContext, useContext, ReactNode } from "react";

// // Create a Context for the currentStage parameter
// const CurrentStageContext = createContext < any > null;

// // Create a custom hook for easy access to the context
// export const useCurrentStage = () => {
//   return useContext(CurrentStageContext);
// };

// export const CurrentStageProvider = ({ value, children }) => {
//   return (
//     <CurrentStageContext.Provider value={value}>
//       {children}
//     </CurrentStageContext.Provider>
//   );
// };
